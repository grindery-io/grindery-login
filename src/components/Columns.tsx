import { Stack, styled } from "@mui/material";

export const Columns = styled(Stack)({
  flexDirection: "row",
  alignItems: "stretch",
  justifyContent: "center",
  flexWrap: "nowrap",
  width: "100%",
  height: "100%",
  flex: 1,
});
